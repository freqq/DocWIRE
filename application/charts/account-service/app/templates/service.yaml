apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - name: http-port
    port: {{ .Values.config.accountService.port }}
    targetPort: {{ .Values.config.accountService.port }}
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}
