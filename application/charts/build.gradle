description = 'charts'

buildscript {
    ext {
        configurationDir = "${project.rootDir}/charts/configuration"
        profilesDir = "${project.rootDir}/charts/values/profiles"
        globalValues = "${project.rootDir}/charts/values/global-values.yaml"
        configurationProfile = "${profilesDir}/configuration/internal.yaml"
        resourcesProfile = "${profilesDir}/resources/internal.yaml"
    }
}