apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - name: http-port
    port: {{ .Values.config.authService.port }}
    targetPort: {{ .Values.config.authService.port }}
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}
