apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - port: {{ .Values.config.authService.port }}
    targetPort: {{ .Values.config.authService.port }}
  selector:
    app: {{ .Chart.Name }}
