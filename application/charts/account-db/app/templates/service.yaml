apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - port: {{ .Values.config.accountDb.port }}
    targetPort: {{ .Values.config.accountDb.port }}
  selector:
    app: {{ .Chart.Name }}
