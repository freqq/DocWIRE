project.ext {
    functionsScript = "${rootProject.rootDir}/../deployment/functions.sh"
    componentValues = "${project.rootDir}/charts/${project.name}/app/values.yaml"
    appNamespace = 'medic-app'
    ingressRepoUrl = 'https://helm.nginx.com/stable'
}

task appLoad()
task appInstall()
