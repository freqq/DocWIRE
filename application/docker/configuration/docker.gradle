project.ext {
    imageTag = 'v1'
    dockerHubName = 'freq'
    registryUsername = 'freq'
    registryPassword = 'zsdcxxx412'
    dockerDirPath = "${project.buildDir}/docker"
    jarName = project.name.replaceAll('_', '-')
}

task dockerPush {}